// [[HANDLE: TEAM_ADMIN_UI]]
import Roles from "../../models/Roles.json" assert { type: "json" };

/** @param {HTMLElement} root @param {TeamMember[]} team @param {(t:TeamMember[])=>void} onChange */
export function renderTeamAdmin(root, team, onChange) {
  root.innerHTML = `…tiny table template…`;
  // only: read inputs, update team[], call onChange. Nothing else here.
}
